# ElevenLabs TTS — key stays SERVER-SIDE (server.mjs), never in the browser.
# Leave blank to use the browser's built-in Web Speech voice instead.
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM   # default = Rachel; swap per agent

PORT=4321
